<?php

use Illuminate\Support\Facades\Route;
use Modules\{{ name }}\Http\Controllers\{{ name }}Controller;

Route::prefix('{{ name | strtolower }}')->group(function () {
    Route::get('/', [{{ name }}Controller::class, 'index']);
});
